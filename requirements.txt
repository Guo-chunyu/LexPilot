# ============================================================================
# LexPilot 依赖（聚合入口，向后兼容保留本文件名）
#
# 本文件安装全部依赖（运行 + 开发/测试 + 机器学习），体积约 1.5 GB。
# 绝大多数用户只需打开网页，请不要安装本文件，改用精简清单：
#
#   开网页 / 跑 API：  pip install -r requirements-runtime.txt
#   开发 / 跑测试：    pip install -r requirements-dev.txt
#   旧 DQN / 向量库：  pip install -r requirements-ml.txt
#
# 依赖分类说明：
#   requirements-runtime.txt  网页规则决策路径最小集（无 torch）
#   requirements-api.txt      = runtime（API 已并入 runtime）
#   requirements-dev.txt      测试与评估工具
#   requirements-ml.txt       旧强化学习 / 本地向量 / 重排模型（约 1.5 GB）
# ============================================================================

-r requirements-runtime.txt
-r requirements-dev.txt
-r requirements-ml.txt
