# LexPilot 可信法律智能技术说明

## 技术定位

LexPilot 是知识增强、证据链驱动的劳动争议智能决策辅助系统。系统不使用强化学习，不训练法律预测模型；核心创新位于知识表示、主动信息获取、时效感知检索和可验证生成。

## 总体流程

```text
用户陈述 / 本地案件材料
        ↓
规则抽取 + 可选结构化语义抽取
        ↓
统一 CaseState 与事实来源追踪
        ↓
事实—证据—法律要件—法条图
        ↓
信息增益主动追问 / 证据补全
        ↓
图扩展法律检索 + 时效与效力校验
        ↓
确定性金额计算与对方抗辩分析
        ↓
事实、引用、时效、要件四重验证
        ↓
有依据生成 / 信息不足拒答 / 人工复核
```

## 1. 证据链 GraphRAG

图节点包括案件、结构化事实、证据材料、法律要件和法律条文。边类型包括 `HAS_FACT`、`SUPPORTS`、`CORROBORATES`、`GOVERNS`、`NEEDS_FACT` 和 `REQUESTED_FOR`。每个法律要件形成一条独立推理路径，并计算：

```text
Support = 0.45 × 事实覆盖
        + 0.35 × 证据覆盖
        + 0.20 × 法源关联
```

发生事实或证据冲突时，路径支持度受到惩罚并禁止生成确定性结论。法律检索以低支持度要件为焦点扩展查询，将结果通过 `matched_elements` 重新接回推理图。

## 2. 信息增益主动追问

每个候选问题保留完整评分分解：法律重要度、预期信息增益、法律要件覆盖、现有证据利用、冲突紧迫度和用户交互成本。系统选择当前价值最高的问题，而不是执行固定问卷；问题文本仍来自可审核的领域配置。

推荐答辩指标：达到同等事实完整度所需轮数、关键事实召回率、重复提问率、问题平均价值分。

## 3. 可验证生成与拒答

生成前校验包含：

1. 事实是否保留来源片段和抽取置信度；
2. 引用 URL 是否属于可信官方域名且具有条文标识；
3. 法条生效/失效日期是否覆盖案件发生日；
4. 每条生成 finding 引用的事实 ID、要件 ID 和法条 ID 是否存在真实图连接；
5. 证据冲突、低覆盖或无有效法源时是否拒绝确定性输出。

语义模型无法直接修改法条、金额或验证结果。模型返回的未知 ID、跨要件错误引用和无来源结论会被确定性验证器删除。

## 模型接口与隐私边界

可选语义模型通过 `backend/ai/provider.py` 的统一 `AIProvider` 接口接入。当前 REST 实现使用 schema-constrained JSON，只承担结构化事实抽取和通过核验后的报告措辞。无密钥、超时、限流、服务错误或 JSON 校验失败时全部回退本地实现。

API 密钥只从忽略提交的 `.env` 读取，不进入前端、会话状态或 API 响应。明显的手机号、身份证号、邮箱和长账号会在远程语义处理前脱敏。原始上传附件默认不发送到外部服务。

## 建议实验

至少准备一组经核对的劳动争议测试问题，比较：

- 关键词检索与图扩展检索的 Top-3 法条准确率；
- 有无时效过滤时的失效法条率；
- 固定优先级与信息增益追问的平均交互轮数；
- 有无生成验证器时的无来源结论率和错误引用率；
- 断网、无密钥和接口超时情况下的流程完成率。
